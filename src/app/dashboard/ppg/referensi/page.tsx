import { importPpgWorkbook } from '../actions'
import { getPpgRows } from '@/lib/ppg/data'
import { PPG_LEVELS } from '@/lib/ppg/scoring'
import { EmptyState, SectionHeading } from '../_components'
import { DeleteImportButton } from './DeleteImportButton'
export default async function ReferensiPage(){const {rows}=await getPpgRows('ppg_import_batches');return <div className="space-y-6"><SectionHeading eyebrow="Sumber kebenaran PPG" title="Referensi & Impor" description="Kelola matriks, jejak asal data, dan impor workbook dengan sanitasi PII."/><div className="grid gap-5 xl:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">Matriks Risiko UPG KPK 5x5</h3><p className="mt-1 text-sm text-slate-500">Skor = kemungkinan × dampak.</p><div className="mt-4 space-y-2">{PPG_LEVELS.map(l=><div key={l.label} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-sm"><span className="flex items-center gap-2 font-semibold"><i className="h-3 w-3 rounded-full" style={{backgroundColor:l.color}}/>{l.label}</span><span>{l.min}–{l.max}</span></div>)}</div></section><form action={importPpgWorkbook} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">Impor Rekapitulasi Pelaporan</h3><p className="mt-1 text-sm text-slate-500">Pilih file .xlsx hasil ekspor KPK. Sistem mencari sheet <strong>Worksheet</strong> dan mendeteksi baris header secara otomatis. Format lama <strong>Worksheet (GOL)</strong> tetap didukung.</p><input className="mt-5 block w-full rounded-xl border border-dashed border-slate-300 p-4 text-sm" type="file" name="file" accept=".xlsx" required/><div className="mt-4 rounded-xl bg-amber-50 p-3 text-xs text-amber-900"><strong>Privasi:</strong> hanya atribut operasional yang dipetakan. Nama penerima, NIK, nama/alamat pemberi, kontak, alamat penerima, dan tanggal lahir tidak disimpan.</div><button className="mt-4 rounded-xl bg-indigo-700 px-4 py-2 text-sm font-semibold text-white">Impor data anonim</button></form></div><section><h3 className="mb-3 font-bold">Riwayat impor</h3>{rows.length?<div className="space-y-2">{rows.map(r=><div key={String(r.id)} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-200 bg-white p-4 text-sm"><div><b>{String(r.nama_file)}</b><p className="text-xs text-slate-500">{String(r.source_sheet)} · {String(r.created_at)}</p></div><div className="flex items-center gap-2"><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold text-indigo-700">{String(r.status)}</span><DeleteImportButton batchId={String(r.id)} fileName={String(r.nama_file)} /></div></div>)}</div>:<EmptyState title="Belum ada riwayat impor" description="Impor pertama akan direkam dengan nama file, sheet, jumlah baris diterima/ditolak, dan waktu proses."/>}</section></div>}
