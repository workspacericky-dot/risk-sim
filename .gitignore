# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# reference assets (not used by the app; fonts live in public/fonts)
/ref

# sampel data kepegawaian riil (NIP, presensi, golongan) — hanya dipakai lokal
# oleh scripts/verifikasi-ca-kepeg.ts, tidak pernah menjadi bagian aplikasi
/CA_Audit_Kepeg

# sampel laporan keuangan satker riil (PDF SAKTI) — hanya dipakai lokal oleh
# scripts/verifikasi-ca-laporan-keuangan.ts, tidak pernah menjadi bagian aplikasi
/CA_Audit Lapkeu/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
